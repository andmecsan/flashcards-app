# URL raíz de la API de Flash Learn (sin /api/v1, ver src/services/api.ts).
# En local no hace falta definirla, cae por defecto a http://localhost:3000.
# En Cloudflare Pages, se configura como variable de entorno del proyecto.
VITE_API_ROOT=http://localhost:3000
